_schema-version: "3.2"
ID: ProductsList
version: 0.0.1
modules:
- name: productslist-approuter
  type: approuter.nodejs
  path: productslist-approuter
  requires:
  - name: ProductsList_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: ProductsList_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: ProductsList_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ProductsList-content.zip
      name: ProductsList
      target-path: resources/
- name: ProductsList
  type: html5
  path: ProductsList
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: ProductsList_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: ProductsList_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
